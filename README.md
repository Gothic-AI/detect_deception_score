# 📊 TensorBoard Usage

Visualize your training progress using **TensorBoard**.

## 1. Install TensorBoard

If you don't have it yet, install via pip:

```bash
pip install tensorboard
```

## Run TensorBoard

```
tensorboard --logdir=logs/
```

## Open in Your Browser

```
http://localhost:6006/
```